# Convex — after `npx convex dev` or from Convex dashboard → Settings → URL
NEXT_PUBLIC_CONVEX_URL=

# Vercel / CI — Convex → Settings → Deploy key (only for production deploys, not local dev)
# CONVEX_DEPLOY_KEY=
